header:
  version: 14

machine: company-qemuarm64
distro: companydistro

repos:
  bitbake:
    url: "https://git.openembedded.org/bitbake"
    branch: "2.8"
    path: "sources/bitbake"
    layers: {}

  openembedded-core:
    url: "https://git.openembedded.org/openembedded-core"
    branch: "scarthgap"
    path: "sources/openembedded-core"
    layers:
      meta:

  meta-openembedded:
    url: "https://git.openembedded.org/meta-openembedded"
    branch: "scarthgap"
    path: "sources/meta-openembedded"
    layers:
      meta-oe:
      meta-python:
      meta-networking:
      meta-filesystems:

  meta-virtualization:
    url: "https://git.yoctoproject.org/meta-virtualization"
    branch: "scarthgap"
    path: "sources/meta-virtualization"
    layers:
      .:

  meta-company:
    path: "/yocto-workspace/meta-company"

target:
  - company-image

local_conf_header:
  cache: |
    DL_DIR = "/yocto-downloads"
    SSTATE_DIR = "/yocto-sstate"
    # Separate volume so tmp can be wiped independently of sources and sstate cache
    TMPDIR = "/yocto-tmp/tmp"

  distro: |
    PACKAGE_CLASSES = "package_ipk"

    INIT_MANAGER = "systemd"

    DISTRO_FEATURES += "systemd"

    VIRTUAL-RUNTIME_init_manager = "systemd"

    EXTRA_IMAGE_FEATURES = ""

    USER_CLASSES ?= "buildstats"

    PATCHRESOLVE = "noop"

    INHERIT += "rm_work"
